> testes para as classes
> abstrair em um objeto classes semelhantes ok
> abstrair o conteúdo do "lines" em um objeto
> melhorar o require
____________________________________________

- parametros nomeados: vantagens melhor legibilidade, entender quando está chamando o método
saber melhor que está sendo feito. a order não importa. saber somente os nomes.
- forma padrão: saber a ordem, mas não sabe o nome
- opções extras para passar para uma função/método
- forma de inicializar uma estrutura de dados deve ser generica suficiente
- quantidade de parametros fica ruim para utilizar
- tipagem haskell + postgrest
- enumerator ruby > uso para subistituir a logica de comparacao
